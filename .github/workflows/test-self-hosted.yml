name: Test Self-Hosted Runner

on:
  workflow_dispatch:

jobs:
  test-1:
    runs-on: arc-runner-x86-spot
    steps:
      - run: echo "Job 1 on $(hostname)" && sleep 30

  test-2:
    runs-on: arc-runner-x86-spot
    steps:
      - run: echo "Job 2 on $(hostname)" && sleep 30

  test-3:
    runs-on: arc-runner-x86-spot
    steps:
      - run: echo "Job 3 on $(hostname)" && sleep 30

  test-4:
    runs-on: arc-runner-x86-spot
    steps:
      - run: echo "Job 4 on $(hostname)" && sleep 30

  test-5:
    runs-on: arc-runner-x86-spot
    steps:
      - run: echo "Job 5 on $(hostname)" && sleep 30

